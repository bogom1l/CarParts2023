
<style>

    .btn-purple {
        background-color: purple;
        color: white;
    }

    .btn-lightgreen {
        background-color: lightgreen;
        color: white;
    }

    .my-custom-car-image-props {
        height: 200px;
        object-fit: cover;
    }

    .my-home-car-image-props {
        height: 250px;
        object-fit: cover;
    }

    .my-details-car-image-props {
        height: 350px;
        object-fit: cover;
    }

    .my-custom-background-color {
        background-color: #e6f9ff;
    }

    .my-custom-background-color-v2 {
        background-color: #ccfff5;
    }

    .my-custom-background-color-v3 {
        background-color: #b3fff0;
    }

    .btn-customColor {
        background-color: darkolivegreen;
        color: white;
    }

    .btn-orange {
        background-color: orangered;
        color: white;
    }

</style>



@*DELETE Part BUTTON*@
<script>
        // Handle the click event for the delete button
        $('.delete-part').on('click', function (e) {
            e.preventDefault();
            const partId = $(this).data('id');
            
            // Show SweetAlert confirmation dialog
            Swal.fire({
                title: 'Are you sure?',
                text: 'You are about to delete this part. This action cannot be undone.',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#d33',
                cancelButtonColor: '#3085d6',
                confirmButtonText: 'Yes, delete it!'
            }).then((result) => {
                if (result.isConfirmed) {
                    window.location.href = `/Part/Delete/${partId}`;
                }
            });
        });
</script>
    


@*DELETE Car BUTTON*@
<script>
        // Handle the click event for the delete button
        $('.delete-car').on('click', function (e) {
            e.preventDefault();
            const carId = $(this).data('id');

            // Show SweetAlert confirmation dialog
            Swal.fire({
                title: 'Are you sure?',
                text: 'You are about to delete this car. This action cannot be undone.',
                showCancelButton: true,
                confirmButtonColor: '#d33',
                cancelButtonColor: '#3085d6',
                confirmButtonText: 'Yes, delete it!'
            }).then((result) => {
                if (result.isConfirmed) {
                    window.location.href = `/Car/Delete/${carId}`;
                }
            });
        });
</script>

                
                
@*END RENTAL Car BUTTON*@
<script>
        // Handle the click event for the delete button
        $('.end-rental').on('click', function (e) {
            e.preventDefault();
            const carId = $(this).data('id');

            // Show SweetAlert confirmation dialog
            Swal.fire({
                title: 'Are you sure?',
                text: 'You are about to end your rental.',
                showCancelButton: true,
                confirmButtonColor: '#d33',
                cancelButtonColor: '#3085d6',
                confirmButtonText: 'Yes, end it!'
            }).then((result) => {
                if (result.isConfirmed) {
                    window.location.href = `/Car/EndRental/${carId}`;
                }
            });
        });
</script>


@*DELETE MY REVIEWS Car BUTTON*@
<script>
    // Handle the click event for the delete button
    $('.delete-myreviews').on('click', function (e) {
        e.preventDefault();

        // Show SweetAlert confirmation dialog
        Swal.fire({
            title: 'Are you sure?',
            text: 'You are about to delete all your reviews.',
            showCancelButton: true,
            confirmButtonColor: '#d33',
            cancelButtonColor: '#3085d6',
            confirmButtonText: 'Yes, delete them!'
        }).then((result) => {
            if (result.isConfirmed) {
                window.location.href = `/Dashboard/DeleteAllReviewsForUser`;
            }
        });
    });
</script>



@*REFUND Part BUTTON*@
<script>
    // Handle the click event for the delete button
    $('.refund-part').on('click', function (e) {
        e.preventDefault();
        const partId = $(this).data('id');

        // Show SweetAlert confirmation dialog
        Swal.fire({
            title: 'Are you sure?',
            text: 'You are about to refund your purchase.',
            showCancelButton: true,
            confirmButtonColor: '#d33',
            cancelButtonColor: '#3085d6',
            confirmButtonText: 'Yes, refund it!'
        }).then((result) => {
            if (result.isConfirmed) {
                window.location.href = `/Part/Refund/${partId}`;
            }
        });
    });
</script>