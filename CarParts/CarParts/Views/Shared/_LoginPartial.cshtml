@using Microsoft.AspNetCore.Identity
@using CarParts.Data.Models
@using CarParts.Services.Data.Interfaces
@using CarParts.Web.Extensions
@using Microsoft.AspNetCore.Mvc.TagHelpers
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@inject IUserService UserService

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
   @* <li class="nav-item">
        <a  class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity?.Name!</a>
    </li>*@

    <li class="nav-item">
        @if (await UserService.GetUserFullNameById(User.GetId()) is var fullName && fullName != null)
        {
            <a class="nav-link text-dark">Hello, @fullName</a> @*//TODO: da go napraq s front end malko po-kwadratno i da maxna twa Hello....*@
        }
        else
        {
            <a class="nav-link text-dark">Hello, @User.Identity!.Name</a>
        }
    </li>



    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}
</ul>
